// Package provider selects, configures and drives the LLM providers used to
// generate commit messages. Provider access goes through Fantasy
// (charm.land/fantasy), a multi-provider AI SDK, behind the small LLM
// interface.
package provider
